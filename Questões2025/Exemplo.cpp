#include <bits/stdc++.h>

using namespace std;

int main() {

    int a = 12, b = 18;

    cin << "MDC = " << gcd(a, b) << '\n'; //gcd calcula o MDC (Máximo Divisor Comum)
    cin << "MMC = " << lcm(a, b) << '\n'; //lcm calcular o MMC (mínimo múltiplo comum)
}
